# AI Usage Log

- 2026-09-01: Fixed the previous-tokamak score, which was 0 for every stack because of two independent bugs: `Load_xlsx` hardcoded `prevTok` to all-False and never read a usage column, and `Load_data` then compared that boolean array against the string `'Y'` (always False), so fixing either alone would still have given zero. `Load_xlsx` now reads the new `Prev_Tok` column (31 of 138 lines flagged, 27 surviving the 1 nm downselect) and warns if the column is missing. Also added `Plot_Zeeman_Estimates` so `Plot_Stack` panels show estimated envelopes for lines with no h5 spectrum (hatched boxes, since only the width is known) instead of leaving those regions blank, plus new `SpeciesLines.py` for a per-species line survey.

- 2026-08-28: Added Zeeman width estimation to StackMaker so uncomputed lines are no longer invisible to the grader. New `Est_Zeeman_Width` (analytic Lande pattern extent for LS terms, validated to median ratio 0.99 against Curt's 80 computed lines with a x1.9 95th-pct safety factor; lambda^2-scaled max-observed fallback for jk/bracket-coupled terms), plus `Parse_Term`/`Parse_J`/`Lande_g`/`Zeeman_Pattern_Extent` helpers with textbook citations. `Load_xlsx` now reads the term/J columns and returns physics-based widths, replacing the unseeded random-filler widths (`Load_data`'s signature is unchanged: the catalog needs the full pre-downselect line list, so it reads `Load_xlsx` directly). New `Merge_Zeeman_Catalog` combines Curt's measured h5 envelopes (162 peaks) with estimates for the 48 uncomputed lines, wired into `Grade_Stack` via `zFolder`/`xlsxFile`. Fixed `Grade_Zeeman` rewarding lines with no Zeeman data (+1 per uncovered line) by clipping the per-line contribution at 0. Then replaced `Grade_Zeeman`'s nearest-point search over a 0.25 nm wavelength grid (kept as `Grade_Zeeman_Grid`) with an exact per-line evaluation: `Zeeman_Line_Scores` counts catalog envelopes containing each line's true wavelength and `Zeeman_Line_LUT` bakes those into a per-species float16-indexed table, so grading is a gather. The grid was coarser than the ~0.2-0.4 nm overlap bands and sampled just outside them, which made the whole W/Mo/Fe/Ni/Cu/Al stack score a flat 0 across all 98.5M combinations; it now spans 0 to -3 and runs in 2.8 s/stack instead of ~100 s.

- 2026-08-07: Verified Curt's fixed Zeeman dataset `broad_all_fixed/` (all 50 files unique/correct vs `broad/`; found 4 all-zero-signal files for ¹P°→¹S term pairs — new bug to report) and switched StackMaker.py to it: added an all-zero-signal skip guard in `Load_H5_Zeeman` (prevents NaN poisoning of the overlap array), made per-file plots respect `plot=`, chunked `Grade_Zeeman` over the combination axis (fixes 4 GB pinned-memory OOM on 6-species stacks), and ran the full 98.5M-combination grading pipeline on the three stacks in `__main__`, saved to `TestStack_broadfixed.npz`.
- 2026-08-06: Fixed `Gen_Rays` in RayTraceFilter.py: the divergence half-angle was being used as the sigma of a Gaussian (RMS divergence 2x too high, unbounded tails), and radial start positions used `rand()` instead of `sqrt(rand())` (density ~1/r, rays piled at beam center). Now samples a uniform-disk far field and an area-uniform beam cross-section. Validated against an exact numerical 90-degree OAP ray trace: RMS half-angle matches to 0.06% and radial fill to 0.02%. Same trace confirmed `Beam_Diam_Angle` divergence is correct (NA-independent, scales as fDiam/RFL) but under-estimates the edge angle by ~21% from OAP off-axis aberration.
- 2026-07-08: Generated `zeeman_duplicate_data_issue.png` explaining the shared-multiplet-spectrum issue in Curt's Zeeman h5 files (C II 513-515 nm example) and its 6x distortion of the Grade_Zeeman overlap score.
- 2026-07-08: Fixed `Interact_Filter` in RayTraceFilter.py mutating the caller's filter plane during transmission (back-surface intersection now uses a local plane); verified bit-identical ray geometry and no numerical change to existing results. Also identified 13 corrupted Zeeman h5 files in `broad/` (wrong species data) pending discussion with Curt.
